extends layout.pug

block içerik
    // Eğer isim girilmiş ise içinde isim olan h2 mesajını göster
    if isim
        h2 Hoşgeldin çekirge #{isim}!
    else
        form(action='/hello', method='post')
            label İsminizi buyurur musunuz: 
                input(type='text', name='kullanıcıadı')  
            button(type='submit') Gönder